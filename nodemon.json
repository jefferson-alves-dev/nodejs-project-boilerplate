{
  "watch": ["src", "tests"],
  "ext": "ts",
  "ignore": ["node_modules", "dist", ".git"],
  "exec": "ts-node --transpileOnly --skip-ignore ./src/api/server.ts",
  "delay": "1000",
  "env": {
    "NODE_ENV": "development"
  }
}
